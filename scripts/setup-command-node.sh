#!/usr/bin/env bash
set -euo pipefail

echo "Bootstrap the Command Node with Docker, uv, pnpm, PostgreSQL, Redis, Ollama, and Caddy."
echo "This scaffold leaves host provisioning commands to a later hardening pass."
