#!/usr/bin/env bash
set -euo pipefail

echo "Bootstrap the Target Node with the intentionally vulnerable lab services."
echo "Keep outbound connectivity disabled and preserve the isolated attack surface."
